import styled from 'styled-components';

export const Footer = styled.footer`
    text-align: center;
    background-color: var(--laranja);
    padding: 1rem 0;

    p {
        color: #fff;
    }
`;
